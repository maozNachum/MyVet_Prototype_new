# MyVet — סגירת P0, 17.09.2026

מצב כולל: **P0 טרם נסגר במלואו; אין אישור למידע אמיתי או ל־Production.**
עבודה ב־`Full_Demo` בתוך `.codex-worktrees/full-demo`; בסיס תחילת העבודה `f92bed3` עם חבילת P0 קודמת שטרם נשמרה ב־commit.

## הושלם בסבב הנוכחי

- אותר מקור מיגרציית הפרטיות `20260909221500` ב־commit `67fd7ae`; מיגרציית concurrency `20260915130000` קיימת מקומית. אין צורך בקבצי ניסן כדי לשחזר מימושים אלה.
- נבדקו חי 26 רשומות מיגרציה ב־`myvet-staging` (`fajwhkgafbjgnbgwsumt`) מול חבילת ה־baseline הקודמת.
- בוצעה ביקורת של 32 פונקציות public ועוד 4 private הנגישות ל־authenticated; חתימות מלאות, grants, MFA וגבולות מרפאה נבדקו. פרטים ב[דוח הביקורת](P0_DEFINER_GRANTS_AUDIT_2026-09-16_HE.md).
- נוספה מיגרציה `20260916182356` לקיבוע search_path של `public.set_updated_at()` ללא שינוי גוף/הרשאות.
- נוספה מיגרציה `20260916182805` לבדיקת MFA לפני replay רפואי ולהגבלת פעולות VetBot למרפאת הבקשה, כולל בדיקת שמות מלאי בתוך המרפאה בלבד. ארבע חתימות ה־RPC נשמרו.
- נוספו בדיקות התנהגות PGlite, קבלת SQL בסכמה מלאה ובדיקת allowlist שעוצרת בכשל. הבדיקות שולבו במסלולי Local, Preview ובחבילת VetBot.
- הוכנה מחדש [חבילת החלטות הפרטיות](PRIVACY_LEGAL_APPROVAL_PACK_HE.md); אינה העתק של קובץ ניסן או אישור מטעמו.
- הוכן [נוהל קבלת SMTP](P0_SMTP_ACCEPTANCE_HE.md); לא נשלחו הודעות ולא נטען PASS למסירה.

## אימות והרצה

שתי המיגרציות הוחלו ונרשמו ב־Staging בלבד. חבילת הקבלה הסתיימה ב־`P0 Preview acceptance passed; Production was not targeted.`

| אימות | תוצאה |
|---|---|
| `npm run test:vetbot` | PASS — 200 בדיקות מקור ועוד 60 בדיקות DB, כולל 7 רגרסיות חדשות |
| `npm run test:supabase-baseline` | PASS — שתי הקמות נקיות, 28 מיגרציות, Auth/Storage, בדיקות גבולות ו־DB lint |
| בדיקות חדשות בסכמה מלאה | PASS ב־Local וב־Staging: MFA ב־replay, שינוי מרפאה, שם מלאי מקביל ו־delegate לישות זרה |
| מטריצת תפקידים, concurrency בשני חיבורים, onboarding | PASS ב־Staging |
| Auth lifecycle, שבע פונקציות Edge עם MFA, Storage HTTP | PASS ב־Staging |
| חסימת anonymous וניקוי נתונים סינתטיים | PASS ב־Staging |
| קטלוג grants לפי חתימה מלאה | PASS — 36 חתימות, ללא הרשאות authenticated/anon בלתי צפויות |
| `npm run build`, `npm run typecheck:ai`, `npm run test:frontend-secrets` | PASS; נשארה אזהרת גודל bundle מוכרת |
| תחביר שלושת סקריפטי PowerShell ו־git diff --cached --check | PASS |

בבדיקת Advisor לאחר ההחלה אין עוד אזהרת search_path. נותרות 32 התראות EXECUTE של RPC מכוונים (נסקרו פרטנית) וחמש הודעות INFO על טבלאות פנימיות ללא policy; הגישה הישירה נשארה חסומה.

במהלך ההרצה תוקן סקריפט ההחלה: בטבלת היסטוריית Staging אין עמודת `created_by`; הרישום משתמש כעת בעמודות `version,name` שנבדקו. ניסיון ראשון החיל את תיקון הטריגר אך נעצר ברישום; ההרצה החוזרת השלימה את הרישום ואת המיגרציה השנייה. לא היה שינוי ב־Production. בדיקת נתונים חדשה נעצרה תחילה על משקל חסר ב־fixture; הנתון הסינתטי תוקן וכל החבילה הורצה מחדש ועברה.

ראיות הרצה מקומיות נשמרו תחת `.codex-work/p0-evidence-20260916` (מחוץ ל־Git); התוצאות המסכמות מתועדות כאן. בדיקות token אינן בדיקות מסירת אימייל.

## חסמים שאינם נסגרים באמצעות תיקון קוד

1. SMTP: נדרשים דומיין בבעלות, ספק SMTP ותיבות בדיקה מורשות; לאחר מכן הגדרה ואימות מסירה של הרשמה/הזמנה/שחזור.
2. פרטיות: פרטי ישות ובעלי תפקידים, אישור חוזים וספקים ומסלול AI, תקופות שמירה והסכמות מאושרות. אין להפעיל מחיקות לפי הצעה לא מאושרת.
3. לאחר מדיניות מאושרת: אכיפה שנבדקה ותרגילי זכויות ואירוע מתועדים. טופס תרגיל ריק אינו PASS.
4. סטטוס ענף Staging חי הוא `MIGRATIONS_FAILED` לצד `ACTIVE_HEALTHY`; לא בוצע reset/rebase כדי לשנות תווית זו. יש לברר את מצב האתחול בנפרד לפני שחרור.
5. חבילת P0 נשמרת כ־checkpoint מקומי ב־Full_Demo, כולל העבודה שקדמה לסבב זה; אין push או merge. עדיין אין release candidate משותף שעבר CI. ניטור, שחזור וקבלה עסקית מלאה הם שערי ההשקה הנוספים, לא הוכחה הנובעת מבדיקות P0.

סליקת הפורטל היא דמו; אין לפרש סימון תשלום כראיית גבייה אמיתית. לא הופעלו יכולות AI חדשות ולא נבדקה איכות ספק אמיתי בסבב זה. Production לא שונה.
